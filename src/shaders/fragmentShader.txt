#version 400 core

//in vec3 colour;
in vec2 pass_textureCoordinates;
out vec4 out_Color;
uniform sampler2D modelTexture;

void main(void) {
	//out_Color = vec4(colour,1.0)/3;
	out_Color = texture(modelTexture, pass_textureCoordinates);
}